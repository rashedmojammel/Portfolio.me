// Layers behind the Parallax section (components/sections/Parallax.jsx).
//
// The original design also had 'image' layers (front/back/mid artwork), but
// those source files were never supplied — see the git history if you add
// artwork later and want to bring them back. Only the 'title' layer, which
// needs no image asset, is wired up for now.
export const parallaxLayers = [
  { id: '3', type: 'title', text: 'Rashedul Alam', yPercent: 40 },
]
